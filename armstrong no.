#include <stdio.h>

int main() {
    int n, sum = 0, k, b = 1;
    
    scanf("%d", & n);k = n;
    while (n > 0) { b=0;
        
        int digit = n%10;
        b = digit*digit*digit;
        sum += b;
        n /= 10;
        

    }
    if(sum == k) printf("arm");
    else printf("no");
}
